import 'package:flutter/widgets.dart';

const double kMediumPadding = 24.0; // Padding lớn hơn mặc định
const double kBottomBarIconSize = 20.0; // Kích thước icon trên bottom bar
const double kDefaultPadding = 16.0; // Padding tiêu chuẩn
const double kItemPadding = 10.0; // Khoảng cách giữa các widget con
const double kMinPadding = 5.0; // Padding nhỏ nhất
const double kTopPadding = 8.0; // Padding phía trên
const double kDefaultIconSize = 18.0; // Kích thước icon mặc định
const BorderRadius kDefaultBorderRadius = BorderRadius.all(Radius.circular(8)); // Bo góc mặc định
